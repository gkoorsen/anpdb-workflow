# Data Directory

Raw datasets and derived indexes are intentionally not tracked in Git.

Expected local inputs include ANPDB, ChEMBL, COCONUT, and derived fingerprint/index artifacts. Archive publication inputs separately with checksums.
